{
  "name": "@succra/sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Succra agent SDK: thin typed client for the agent gateway (signing, pre-flight, submit). Runs in the agent's process; never stores keys server-side.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@succra/shared": "workspace:*",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
