{
  "name": "@succra/sdk",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Succra SDK placeholder (Phase 0, no product logic). Agent gateway SDK lands in a later phase per IMPLEMENTATION_PLAN.md.",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
