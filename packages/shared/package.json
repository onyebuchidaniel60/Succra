{
  "name": "@succra/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Succra shared contracts/types placeholder (Phase 0, no product logic).",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
