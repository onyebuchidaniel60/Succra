# `programs/succra` (Phase 0 placeholder)

No Solana instructions, accounts, or program logic. No mission, vault, policy,
transfer, quarantine, succession, or cancel logic is implemented.

The Rust + Anchor 0.32.1 + Solana 2.3.0 program foundation lands in **Phase 1**
per `IMPLEMENTATION_PLAN.md`. Program tests land with it (acceptance: owner can
create/fund/cancel a mission on the local validator/devnet test path).

Toolchain pins (per `ARCHITECTURE.md` §9): Anchor 0.32.1, Solana 2.3.0.
