[package]
name = "succra"
version = "0.1.0"
edition = "2021"
description = "Succra mission continuity program: mission/vault accounts with create/fund/cancel (Phase 1) and agent action execution (Phase 2)."

[lib]
crate-type = ["cdylib", "lib"]
name = "succra"

[features]
default = []
idl-build = ["anchor-lang/idl-build", "anchor-spl/idl-build"]

[dependencies]
anchor-lang = "0.32.1"
anchor-spl = "0.32.1"
