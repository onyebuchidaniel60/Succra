# `supabase/` (Phase 0 placeholder)

No database tables, schema, migrations, RLS policies, or seeds are implemented.
Database + Supabase Auth land in **Phase 3** per `IMPLEMENTATION_PLAN.md`
(acceptance: user A cannot read/update user B's private mission data).

Approved locations per `ARCHITECTURE.md` §22:

```text
supabase/
├── migrations/
└── seed/
```
