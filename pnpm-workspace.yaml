packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 10+ requires explicit approval for postinstall build scripts.
# esbuild's postinstall validates its platform binary (needed by vitest/vite).
allowBuilds:
  esbuild: true
