# `tests/` (Phase 0 placeholder)

No integration, E2E, or security tests are implemented. The required
golden-path E2E, race/replay tests, and security review land in **Phase 9**
per `IMPLEMENTATION_PLAN.md`. Unit/program-test harnesses (vitest workspaces)
are verified by the Phase 0 foundation smoke tests.

```text
tests/
├── integration/
├── e2e/
└── security/
```
