# `@succra/web` (Phase 0 placeholder)

No product logic. No screens from `PROJECT_SPEC.md` §14 are implemented.

The Next.js App Router + TypeScript + Tailwind scaffold lands in a later phase
per `IMPLEMENTATION_PLAN.md` and `ARCHITECTURE.md` §§9/22. Approved structure:

```text
apps/web/
├── app/
├── components/
├── lib/
├── styles/
└── tests/
```
